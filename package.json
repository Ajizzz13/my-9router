{
  "name": "9router-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install -g opencode-ai",
    "start": "9router --port $PORT --host 0.0.0.0 --tray"
  },
  "dependencies": {
    "9router": "latest",
    "opencode-ai": "latest",
    "better-sqlite3": "^11.8.1",
    "sql.js": "^1.12.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
